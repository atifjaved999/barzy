<div class="row">
  <div class="col-lg-12">
    <h1 class="page-header">New Bar</h1>
  </div>
  <!-- /.col-lg-12 -->
</div>

<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                Bar Form
            </div>
            <div class="panel-body">
                <div class="row">
                  <%#= form_with(model: @admin_bar, url: admin_bars_path, local: true, multipart: true) do |f| %>
                  <%= nested_form_for ([:admin, @admin_bar]), :html => {multipart: true} do |f| %>
                    <%= render "form_body", f:f %>

                  <% end %>
                </div>
                <!-- /.row (nested) -->
            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
    </div>
    <!-- /.col-lg-12 -->
</div>

