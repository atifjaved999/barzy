  <% if @admin_product_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_product_category.errors.count, "error") %> prohibited this product category from being saved:</h2>

      <ul>
      <% @admin_product_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="col-lg-12">
  <div class="col-lg-6">
      <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :name, :class=>"form-control", :required=>true,  placeholder: 'Name' %>
      </div>

  </div>
  <div class="col-lg-6">
    <div class="form-group">
      <%= f.label :description %>
      <%= f.text_area :description, :class=>"form-control",  placeholder: 'Description' %>
    </div>
  </div>
</div>
<div class="col-lg-12">
  <%= render "admin/product_categories/product", f:f %>
</div>
<div class="col-lg-12">
<br><br>
  <div class="col-lg-6">
    <%= link_to 'Back', admin_bar_menu_path(params[:bar_id], params[:menu_id]), :class=> "btn btn-default " %>
    <button type="reset" class="btn btn-primary ">Reset</button>
    <button type="submit" class="btn btn-success ">Submit</button>
  </div>
</div>